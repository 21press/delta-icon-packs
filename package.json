{
  "name": "delta-icon-packs",
  "version": "1.0.0",
  "private": true,
  "description": "Public catalog of SVG icon packs for Delta Icon Manager",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build:index": "node scripts/build-index.mjs",
    "build:index:force": "node scripts/build-index.mjs --force",
    "zip": "bash scripts/pack-zip.sh",
    "zip:basics": "bash scripts/pack-zip.sh delta-basics",
    "check": "npm run validate && npm run build:index && git diff --exit-code -- index.json",
    "prepare:pack": "npm run validate && npm run build:index && npm run zip --",
    "release:prep": "npm run validate && npm run build:index"
  }
}
